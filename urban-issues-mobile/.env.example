# Copy this file to .env and edit as needed.

# Set to "false" to use the real API gateway instead of local mock data
EXPO_PUBLIC_USE_MOCK=true

# Real gateway base URL (only used when USE_MOCK=false)
EXPO_PUBLIC_API_URL=https://api.urbanpulse.example.com/v1
