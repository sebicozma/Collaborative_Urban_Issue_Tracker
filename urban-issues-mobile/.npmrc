# Temporary: the corporate npm mirror (nimbus-internal) was unreachable during the
# dev-build setup, so this project points at public npm. Remove this file (or revert
# to the corporate registry) once the mirror is reachable again.
registry=https://registry.npmjs.org/
